import { UserSchema } from './UserSchema';
import { TweetSchema } from './TweetSchema';
import { DefaultSchema } from './DefaultSchema';

/**
 * @description holds all schemas
 */

export const schema = [DefaultSchema, UserSchema, TweetSchema];
